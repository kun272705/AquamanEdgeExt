
export class DouyinChannelDownloader {

  ext;

  constructor(ext) {

    this.ext = ext;
  }

  async discoverTask(conversation) {

    // console.log(Date.now() / 1000, conversation);
  }

  async startExecution(execution) {

    // console.log(Date.now() / 1000, execution);
  }

  async stopExecution(execution) {

    // console.log(Date.now() / 1000, execution);
  }
};
